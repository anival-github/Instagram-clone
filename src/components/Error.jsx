import img from "../error.png";

const ErrorMessage = () => {
  return (
    <div>
      <img src={img} alt="error">
        {img}
      </img>
      {img}
    </div>
  );
};

export default ErrorMessage;
